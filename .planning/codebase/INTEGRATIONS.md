# Integrations
- Excel workbook input via `openpyxl`.
- YAML assumptions I/O via `pyyaml`.
- Interactive UI runtime via `streamlit`.
- Charting via `plotly`.
- Browser screenshot automation via `playwright` (dev tooling).
